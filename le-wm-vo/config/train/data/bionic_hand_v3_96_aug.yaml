dataset:
  num_steps: ${eval:'${num_preds} + ${history_size}'}
  frameskip: 3
  name: d:/ai_training/bionic_hand_dataset_v3_96_aug.h5
  keys_to_load:
    - pixels
    - action
  keys_to_cache:
    - action
